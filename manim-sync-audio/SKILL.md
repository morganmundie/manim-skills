---
name: manim-sync-audio
description: (Not yet implemented) Intended for syncing Manim animation timing to an audio track or voiceover — e.g. timing self.wait() durations and animation run_times to match narration or music beats. Currently a placeholder; do not rely on this skill for real guidance yet.
---

# Manim: Audio Sync (placeholder)

This skill is intentionally empty for now — it's reserved for a future workflow covering things like:

- Timing `self.wait()` calls and animation `run_time`s to match a voiceover script or audio waveform.
- Using manim's `add_sound()` / audio-adjacent tooling to attach a track to a rendered scene.
- Working with Manim Voiceover or similar plugins for TTS-driven timing.

Until this is filled in, fall back to general reasoning and mention to the user that dedicated audio-sync support isn't built out yet.
